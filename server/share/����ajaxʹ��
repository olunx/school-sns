
在submenu.jsp页面和需要在content中显示的内容	中的链接都需要加上	target="content"

例如：
	<a target="content" href="xxxx">测试</a>


在需要提交表单的地方都需要加上	onSubmit="post(this);return false;"
method属性可以不写

例如：
	<form onSubmit="post(this);return false;" action="xxxxx">
	</form>