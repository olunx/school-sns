修改struts的配置文件

把package的extends属性的值，由 struts-default 改成 default 。

例如：
没有验证：
<package name="" extends="struts-default" namespace="/">

有验证：
<package name="" extends="default" namespace="/">
